# Project Instructions

- Do not use Angular `FormBuilder` in this project.
- Prefer explicit `FormGroup` and `FormControl` construction for reactive forms.
- Do not use CSS files; use SCSS for styles.
- Use Conventional Commits for every commit: `type(scope): subject`, or `type: subject` when a scope adds no value.
